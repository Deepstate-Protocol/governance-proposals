# Proposal deployment scripts

Each `DGP-NNN` proposal has one dedicated `DGPNNN/Deploy.s.sol` script in this directory for payload generation,
submission, post-submission verification, execution, and fresh-RPC post-execution verification. Start from
`templates/proposal/Deploy.s.sol.template`; do not share a concrete deployment script between proposals.
