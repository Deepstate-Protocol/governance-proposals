# Rewarder V2 system-condition coverage

This matrix binds every production condition to an executable stateful invariant, a focused fuzz/property test, the
live preflight, or an explicit release responsibility. An EVM invariant can prove how reviewed bytecode behaves under
modeled calls; it cannot prove future external upgrades, honest governance, key custody, market legitimacy, timely
human action, or the provenance of a broadcast transaction.

## Automated state machines

| Surface | Stateful properties |
| --- | --- |
| Deterministic deployment | [`DeployRewarderV2SystemInvariant.t.sol`](../test/invariant/DeployRewarderV2SystemInvariant.t.sol) proves the fixed four-contract CREATE2 plan, checked runtime/immutable/scalar state at occupied targets, both-Controllers-before-Factory dependency order, partial-release recovery, idempotent retries, no writes to external protocol dependencies, explicit confirmation, wrong-chain rejection, and failure on incompatible occupied addresses. The Bootstrap constructor depends only on the Governor, DEEP, and the live legacy Rewarder; it does not depend on the Minter Controller. |
| Frozen endowment mint | [`DGP001Bootstrap.t.sol`](../test/DGP001Bootstrap.t.sol) proves constructor-time floor arithmetic, the fixed endowment amount, exclusion of later legacy accrual, Governor-only minting, exact mint delivery to the Governor, natural token-role failure with atomic rollback, and that repeat calls remain possible only while the temporary token role remains. [`DGP001BootstrapSablierIntegration.t.sol`](../test/DGP001BootstrapSablierIntegration.t.sol) models the Governor's grant-mint-revoke-approve-create sequence through the real Sablier Lockup implementation, checks the exact one-year stream with the Governor as funder and sender, and proves a failed Sablier call rolls the whole sequence back. |
| Composed activation behavior | [`DeepstateActivationConditionsInvariant.t.sol`](../test/invariant/DeepstateActivationConditionsInvariant.t.sol) gives the standalone Bootstrap temporary DEEP mint authority, mints the deployment-frozen endowment to the Governor, explicitly revokes that authority in the next governance action, has the Governor approve Sablier and create the stream directly, establishes sole Minter Controller token administration, directly clears the reviewed V1 hook and uses ordinary Factory deployment for the fully funded 100-million V2, appoints the operator last, and then executes the exact 10-million volunteer allocation. Random unauthorized callers attack every continuing authority while the economic and permission state remains invariant. The literal fifteen-call calldata and order, including the initial no-op deployer re-revocation, are covered by [`test/proposals/DGP001/Proposal.t.sol`](../test/proposals/DGP001/Proposal.t.sol). |
| Minter lifecycle | [`DeepstateMinterSystemInvariant.t.sol`](../test/invariant/DeepstateMinterSystemInvariant.t.sol) randomizes the reusable Controller's sole-admin activation, delegated mint roles, ownership changes, successful and failed Sablier mints, burns, time, the 3-billion maximum supply, deadline behavior, and permissionless administration return. It contains no legacy Rewarder, one-off endowment model, or lifetime gross-issuance counter. |
| Factory lifecycle | [`DeepstateFactorySystemInvariant.t.sol`](../test/invariant/DeepstateFactorySystemInvariant.t.sol) randomizes canonical and invalid token pairs, zero-address native pairs, token decimal scales, both whole-unit maxima and side flags, callers, cooldowns, controller roles, Router custody, existing hooks, Sablier failure, Minter cap exhaustion, independent unlinks and burns, ownership changes, and same-pair replacement launches. Every failed lifecycle call must roll back all observed state, and focused scenarios force compound success/failure branches to be reached. |
| Rewarder operation | [`DeepstateRewarderOperationalInvariant.t.sol`](../test/invariant/DeepstateRewarderOperationalInvariant.t.sol) randomizes hook transitions, caller/pool/token binding, both emission schedules, claim solvency, claimant registration, owner-only balance burns, and real Router maker/taker matching against reverting and out-of-gas hooks. [`DeepstateRewarderV2.t.sol`](../test/DeepstateRewarderV2.t.sol) proves that burning adds no lifecycle gate and that unlinked accounting and claim distribution can succeed after a direct refill. |

All stateful handlers use an explicit selector allowlist. Expected failures are caught inside the handler and must leave
state unchanged, while `foundry.toml` sets `invariant.fail_on_revert = true` so an unexpected handler revert fails the
campaign instead of silently discarding the action and its rolled-back evidence.

## Condition matrix

| ID | Production condition | Enforcement and executable evidence |
| --- | --- | --- |
| D-01 | Deploy only on Robinhood Chain ID 4663. | `DeployRewarderV2System` rejects every fuzzed non-4663 chain; `make check-live` proves the positive live case. |
| D-02 | Governor, DEEP, Router, legacy Rewarder, USDG, Sablier, Comptroller, Safe, and CREATE2 deployer match the reviewed identities and proxy configuration. | Deployment codehash/identity checks plus `script/check-live-deployment.sh`; legacy pool/token/schedule identity, proxy slots, Safe state, Sablier fees, and full role history are intentionally checked by `make check-live` because they are live external state. |
| D-03 | Build from the exact reviewed compiler, source, and pinned submodules. | CI formatting/lint/dependency/build gates and deployment hash tests. A reviewer must still require a clean merged/tagged commit; the EVM cannot inspect Git provenance. |
| D-04 | A predicted target is empty or already contains the reviewed release state. | Empty targets and runtime/immutable/scalar checks are enforced by the deployment state machine and incompatible-occupant property. Because Solady Controller roles are non-enumerable, accepting an occupied Controller also requires an independent scan of every `RolesUpdated` event from its deployment block; the EVM verifier alone cannot prove that an unknown address has no role. |
| D-05 | Partial deployment is recoverable and a retry is idempotent. | Arbitrary partial-order deployment and recovery state machine using the canonical proxy runtime and the real release init code. |
| D-06 | Deployment grants no governance permission and changes no external protocol state. | Deployment invariant records storage accesses and rejects writes to DEEP, Router, legacy Rewarder, USDG, Sablier, Governor, Safe, and other pinned dependencies; it also checks the Bootstrap's immutable Governor and DEEP plus its constructor-frozen amount, confirms it has no token role, and checks both Controllers have zero roles while the Controller deadline and Factory operator/cooldown remain pristine after every deployment sequence. |
| B-01 | At deployment, the Bootstrap fixes `floor((legacy token0 totalAccrued + legacy token1 totalAccrued) * 30 / 100)` in constructor-only storage. Later legacy accrual during voting cannot change it. | Focused constructor, rounding fuzz, fixed-value getter, and later-accrual tests. The reviewed Rewarder identity and counter inputs are verified as offchain deployment release gates rather than enforced by the helper. |
| B-02 | Runtime `mint()` is callable only by the immutable Governor and mints exactly the fixed amount directly to the Governor. It performs no Router, Rewarder, Minter Controller, Sablier, cap, idle-state, dust, or identity validation. | Focused authorization, exact recipient/amount, missing-token-role rollback, and later-accrual-independence tests. |
| B-03 | DGP-001 grants the Bootstrap DEEP's token-level minter role immediately before its one intended mint and revokes that role immediately afterward. The Governor then approves Sablier and creates the one-year stream directly as both funder and sender; a later failure rolls the entire atomic batch back. | Focused repeat-call test proves the helper itself has no one-use latch and can repeat only while the role remains. The Governor-style real-Sablier integration proves grant-mint-revoke-approve-create behavior, exact stream fields, and Sablier-failure rollback. Operational one-use comes from proposal execution plus immediate role revocation. |
| A-00 | Release operators verify the live legacy Rewarder, Router, pool, two pool tokens, deployment-time accrued counters, and sufficient headroom beneath the 3-billion maximum supply before deployment and again before submission/execution where applicable. | Deployment scripts, `make check-live`, composed activation modeling, and pinned-archive fork preconditions provide release evidence. These are offchain release gates; the Bootstrap only freezes the amount derived from the two counter reads and the Governor payload does not revalidate identity, idleness, or cap headroom. |
| A-01 | DGP-001 explicitly re-revokes the sole historical external minter, then grants the Bootstrap role, mints the frozen endowment, revokes the role, directly creates the Governor-funded Sablier stream, and activates the Minter within the same fifteen-call atomic Governor batch. | Exact payload-order test, real-Governor fork lifecycle, exhaustive live role-history scan, combined activation invariant, and forced final-deployment rollback test. |
| A-02 | The exact live role history proves no bypass DEEP minter exists before execution; the proposal explicitly revokes its temporary Bootstrap role before the Minter Controller becomes the sole token-level minter. | Composed activation model and DGP-001 intermediate-role assertions. The live role-history checker supplies the exhaustive production inventory because OpenZeppelin roles are not enumerable onchain and verifies the exact five-event execution sequence. |
| A-03 | `activateTokenAdministration()` succeeds only with the Minter Controller as DEEP's sole default administrator and without a preexisting Controller token-minter role; it self-grants that role and starts the 730-day term without minting or creating a stream. | Minter activation/deadline/rollback invariants, focused Controller tests, Bootstrap-to-Minter integration property, and DGP-001 fork assertions. |
| A-04 | The volunteer primary allocation is exactly 10 million DEEP split into the three dictated amounts. | Exact activation economic invariant; the three balances sum exactly to 10 million base-unit precise DEEP. |
| A-05 | Each of the three volunteer mints creates `floor(primary * 30 / 70)` in its own one-year Inc stream. | The exact activation invariant checks streams 3 through 5, each containing `1,428,571.428571428571428571` DEEP, and proves the independently rounded total is `4,285,714.285714285714285713` DEEP. |
| A-06 | Factory receives only Controller-local mint authority and V1 hook authority; V1 Controller owns Router; approved Safe is operator. | Exact activation authority invariant and Factory authority state machine. Factory is asserted never to hold a DEEP token role. |
| A-07 | The Router fee and two active hook flags are preserved while the exact V1 hook is replaced by the exact Factory-created V2. | Composed activation model checks the 10-bps fee, packed flags, predecessor, returned/emitted V2 address and configuration, funding, and permissions. DGP-001 checks the public hook and fee state; its final archive test must also inspect the Router's packed side flags. |
| A-08 | Release operators execute DGP-001 only after confirming the pool has no bid/ask top and V1 has no token0/token1 cursor. The direct hook clear and ordinary Factory deployment are atomic, but neither the Bootstrap nor the Governor payload enforces the idle-state gate. | Live preflight, exact activation composition model, real-Governor lifecycle, and forced final-deployment rollback test provide release evidence. Any drift after preflight requires stopping and rebuilding the release; the final unmocked deployed-state archive run remains required. |
| M-01 | Controlled minting is inactive before lock and at/after the exact 730-day deadline. | Minter phase, strict-window, exact-deadline, and permanent-expiry properties. |
| M-02 | Only the Controller owner or explicit Controller-local minters can mint. | Random caller/role/ownership Minter state machine and exact activation unauthorized-caller campaign. |
| M-03 | Every primary mint produces `floor(primary * 30 / 70)` in an independent one-year, zero-cliff, noncancelable, nontransferable stream. | Per-stream Minter invariant over every successful randomized mint. Rounding is below exact 30% only by less than one DEEP base unit when the primary amount is not divisible by seven. |
| M-04 | Every successful controlled mint leaves live DEEP supply at or below the immutable 3-billion maximum. Burns reduce total supply and restore exactly that much live-supply headroom; there is no lifetime gross-issuance accounting. | Minter maximum-supply and burn-headroom invariants plus Factory mint-conservation coverage. |
| M-05 | A failed token/Sablier interaction is atomic. | Random Sablier failure state and focused failure/recovery property assert unchanged supply, balances, allowance, and stream ID. |
| M-06 | Controller ownership cannot become zero/self; expiration returns DEEP admin to the current owner. The DGP-001 administration handoff relies on Governor-batch atomicity rather than a one-off recovery entrypoint. | Random invalid ownership, governance rotation, exact-deadline return, permissionless unlock, and proposal rollback properties. |
| M-07 | Factory ownership is independent from both Controller owners. Market operations rely on the Factory owner/operator gate plus the Controller-local roles and Router custody required by the underlying calls; missing permissions revert at those calls. | Constructor and ownership-transfer independence tests, Factory authorization state machine, and explicit dependency-removal/restoration properties. |
| F-01 | Only the Factory owner/current operator can deploy, unlink, or request a Rewarder balance burn; only the owner can change the operator. | Random caller Factory invariants and full rollback assertions. |
| F-02 | Deployment and unlinking require the V1 Controller's hook-manager role and Router custody; deployment additionally requires the Minter Controller's local mint role. The independent balance-burn path requires neither Controller permission, but the supplied Rewarder's owner check must accept the Factory. | Activation proves the initial grants/custody. Factory roles/custody are independently removed and restored, and deploy, unlink, and burn paths are exercised under their exact dependencies. Continued retention is live governance state and must be monitored. |
| F-03 | The caller supplies canonical `token0 < token1`. Each nonzero token must return decodable `decimals()` metadata, while `address(0)` is allowed only as the lower native-asset token and uses 18 decimals. Both one-unit and maximum scaled quantities must fit `uint160`. | Equal/unsorted pairs, malformed metadata, native-zero handling, 6/8/18-decimal cross-scaling, and representability-boundary properties cover deployment. Token identity, metadata truthfulness, and economics remain external review conditions. |
| F-04 | The Factory passes token order and both flags through without sorting or semantic remapping; both sides may intentionally be inactive. | Random direct side flags, including `false/false`, are compared with the Router's actual packed token0/token1 activation state after every successful launch and in rollback snapshots. |
| F-05 | The operator supplies `token0MaxUnits` and `token1MaxUnits` in whole units. Every side starts at one whole unit; `deployMarket` independently scales each side from its live decimals or the native 18-decimal rule. The inherited Rewarder enforces a 1,000x minimum and the Factory enforces a 1,000,000x maximum. | Per-side boundary/adversarial state machine, cross-decimal and native scaling checks, and immutable Rewarder configuration invariant. |
| F-06 | An ordinary launch may replace any existing Router hook. The Factory stores no deployment history, active-Rewarder, or reverse-provenance registry, and replacement does not alter the predecessor contract or balance. | Random external-hook mutation, deployment, unlinking, and same-pair replacement state machine; the Router hook and returned/emitted Rewarder address provide current and transaction-scoped evidence. |
| F-07 | The global three-day cooldown and the Minter Controller's maximum supply bound launches. The Factory has no independent funding budget or committed-funding counter. | Independent literal policy assertions, timestamp sequences, same-pair relaunch scenarios, and Minter cap-exhaustion rollback properties. Unlinking and burning do not reset the global deployment deadline, while a burn restores live-supply headroom. |
| F-08 | Every market atomically deploys, fully funds with 100 million primary DEEP, creates the paired Inc stream, and installs a 50-million-per-side, 365-day hook using the caller's exact pair and flags. | Factory configuration and mint-conservation invariants inspect each returned/emitted Rewarder, current Router hook, and stream's funder, recipient, amount, one-year duration, zero unlocks, and transfer/cancel flags. |
| F-09 | Factory replacement is intentionally unconditional on current-hook identity. After offchain identity and idle-state release gates pass, DGP-001 still performs its dictated one-time replacement as an exact Governor-level sequence: direct Router clear, then ordinary `deployMarket`, all in one atomic batch. | Existing-hook replacement properties plus the live preflight and exact DGP-001 payload-order, intermediate-state, lifecycle, and rollback tests. The direct clear is not a Factory prerequisite, and the payload does not repeat the identity or idle checks. |
| F-10 | Mint-window, cap, Sablier, role, Router, configuration, unlink, or burn failure rolls back that call completely. | Snapshots cover the Factory cooldown deadline, Router hook, DEEP supply/balances, stream IDs, and Rewarder ownership. Unlink and burn are tested as independent transactions with no assumed coupling. |
| F-11 | Factory logs distinguish Rewarder creation and primary funding; Router configuration is the authoritative record of installation and unlinking. No event or mapping claims that predecessor accounting state moved, that a hook is still active, or that unlinking burned a balance. | Focused lifecycle tests require `RewarderDeployed` and `RewarderFunded`, inspect Router configuration events/state for hook changes, and use the Minter Controller's separate `MintedWithVesting` event for the stream ID. |
| R-01 | Only Router can execute and only for the immutable pool and pool tokens. | Random unauthorized/wrong-pool/wrong-token calls with unchanged cursor/accounting state. |
| R-02 | Each enabled V2 side activates independently, remains monotonic, and never exceeds its 50-million cap or the 100-million combined cap over 365 days. | Rewarder schedule/accrual state machine and fuzzed monotonic/quantity-adjusted preview properties. Separate reproduction tests intentionally retain V1's immutable 500-million/395-day parameters. |
| R-03 | Reviewed `execute()` paths fit Router's 200,000-gas hook budget. | Randomized successful-execution gas invariant plus a deterministic cold accrual call that succeeds with exactly 200,000 gas forwarded. This must be rerun after compiler, source, or gas-schedule changes. |
| R-04 | An underfunded claim fails atomically and succeeds after funding. | Fuzzed live-order claim rollback/retry property. |
| R-05 | Claimant registration before order deletion preserves a claim; an unregistered deleted owner cannot be reconstructed. | Paired registered/unregistered fuzz property using the Rewarder's real claim path. |
| R-06 | Router matching survives a reverting or out-of-gas hook, while the failed transition records no reward update. | Real Router fuzz properties execute an opposing maker/taker match with token exchange for both hook failures. This proves trading isolation and makes the missed-accounting behavior explicit. |
| R-07 | Only the Rewarder owner can burn its complete current reward-token balance; the burn does not add a lifecycle state or disable inherited accounting. | Stateful owner/unauthorized-call invariant and focused exact-balance, zero-balance, and direct-refunding properties. |
| R-08 | Unlinking a Rewarder stops future Router hook callbacks but does not retire, burn, or disable its inherited accounting. The optional Factory balance burn does not unlink it. If a stored cursor still matches the live order-book top, permissionless `distributeRewards` can lazily accrue after unlinking; after a separate burn, a positive payment reverts atomically until a direct refill funds it. | Independent unlink-without-burn and burn-without-unlink tests prove the separation. Focused V2 burn/refill coverage proves there is no lifecycle gate and that accounting and claims remain usable after direct refill. |
| E-01 | Minting stops at day 730 and anyone can return administration atomically to the current Controller owner. | Exact-boundary Minter property and stateful expiry invariant. |

## External conditions that tests cannot make true

The suite exercises failure behavior for these assumptions but cannot guarantee their future truth:

- Sablier Lockup and its upgradeable Comptroller remain compatible, available, `nativeToken != DEEP`, and zero-fee for
  the call used by the Minter. `make check-live` and the real Sablier fork test must run immediately before deployment,
  proposal submission, execution, and material minting.
- The Governor remains honest and functional, voters pass the intended payload, the proposer retains sufficient
  delegated STATE votes, and governance is not captured.
- Any pre-existing deterministic Controller target has a complete `RolesUpdated` event inventory proving there are no
  unknown delegated role holders. Runtime code and scalar getters cannot enumerate Solady's address-keyed role mapping.
- The Deepstate Inc Safe and deployment/proposer keys remain secure and available. Preflight verifies the expected Safe
  singleton, owner, threshold, and modules only at one snapshot.
- Each operator-selected pair has suitable token/native behavior, truthful decimal metadata for nonzero tokens,
  liquidity, and economically correct whole-unit maxima. The Factory accepts generic canonical pairs, treats
  `address(0)` as an 18-decimal native asset, and cannot prove metadata truthfulness or the legal/economic identity of
  either asset.
- Governance reserves sufficient headroom beneath the maximum supply for each fully funded 100-million market and its paired stream.
  Tests prove cap failure and rollback behavior, not that future headroom will remain available.
- Before DGP-001 execution, users or keepers register all V1 claimants that must survive order deletion and empty both
  sides so the final V1 callbacks clear its cursors. The live preflight observes this state, but neither the Bootstrap
  nor the Governor payload enforces it; any drift after the check requires stopping the release.
- V1's unused balance remains permanently in the immutable legacy contract because it has no retirement or sweep path.
- Users or keepers register claimants before order ownership is deleted, and the trusted operator neither unlinks a
  needed hook nor independently burns its Rewarder balance before the intended claim window closes.
- Governance or its monitor keeps each intended active Rewarder installed as the Router hook with the reviewed side
  flags. An authorized hook replacement stops future Router callbacks for that side, but it does not disable inherited
  permissionless distribution: a stored cursor that still matches the live order-book top can continue accruing lazily,
  and a positive payment can succeed after the burned Rewarder is directly refilled.
- A broadcaster has the correct wallet, gas, nonce, RPC, and explicit `--broadcast` intent; receipts, finality, explorer
  verification, and the release manifest are reviewed and recorded.
- Independent human review, a clean merged/tagged commit, and source/submodule provenance are release-process facts,
  not EVM state.

DGP-001 now has a concrete fifteen-action script, pinned payload/description checks, submission and execution preflights,
intermediate authorized-call assertions, immediate postconditions, a real-Governor lifecycle, and an atomic rollback
test. The Bootstrap amount is fixed from the legacy counters at deployment, so accrual during voting is intentionally
excluded; the Governor is the endowment stream's funder and sender. Its current fork models the not-yet-completed
operational idle handoff and locally deploys the four deterministic targets; release readiness still requires actual
deployment receipts, naturally idle live state, and a final unmocked run against a pinned Robinhood archive block.
DGP-002 has a concrete three-action script, exact per-recipient mint and
per-call vesting arithmetic, real-Governor DGP-001-then-DGP-002 execution, three-stream assertions, immediate
postconditions, and a final-mint rollback test. Its sequential fork inherits DGP-001's pre-deployment and mocked-idle
limitations; release readiness requires repinning it to a real archive block after production DGP-001 execution.

## Commands

```bash
make invariants
make check
make check-live
```

`make invariants` is offline and includes deterministic, fuzz, and stateful tests under `test/invariant/`.
`make check-live` is a read-only production snapshot and real-Sablier fork gate. Both are required because neither can
prove the other's class of conditions.
